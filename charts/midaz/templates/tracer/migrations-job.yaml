{{- if and .Values.tracer.enabled .Values.tracer.migrations.enabled }}
{{- /*
Tracer schema migration runner.

midaz v4 removed in-process migration from the tracer: the service boots against
an already-migrated database, and the release publishes a dedicated
`midaz-tracer-migrations` image (FROM migrate/migrate) whose entrypoint applies
the migration set from DB_* (or DATABASE_URL) and exits.

The Job name carries the migration image tag because a Job spec is immutable: a
stable name would leave the original Job in place on upgrade and silently skip
the new migrations. Re-running is safe — golang-migrate keeps its progress in
the schema_migrations table, not on disk.
*/}}
{{- /* Job spec knobs (backoffLimit/activeDeadline/ttl/annotations) come from here; the
   container specs and their DB_* resolution are shared with the tracer pod's
   initContainer via midaz-tracer.migrations{WaitForPostgres,Runner}Container so the
   Job and the init migrate a byte-identical database. */ -}}
{{- $migrations := .Values.tracer.migrations }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "midaz-tracer.migrationsFullname" . }}
  labels:
    {{- include "midaz-tracer.labels" (dict "context" . "name" (printf "%s-migrations" .Values.tracer.name) ) | nindent 4 }}
  annotations:
    {{- /* Run the migration as an ArgoCD Sync hook so it is excluded from the app's
       desired-state diff — same rationale as the ledger migrations Job. Tag-pinned
       names left prior releases' Jobs OutOfSync ("ignored (requires pruning)"), so
       the app never reached Synced and the sync-wait gate timed out. A hook never
       enters the diff; HookSucceeded removes the Job on completion and
       BeforeHookCreation clears a same-named leftover before recreate (Job spec is
       immutable). Sync phase keeps it in the same wave as the password Secret, so
       fresh installs are unaffected. Re-running is idempotent (schema_migrations). */}}
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
    {{- /* Plain helm install/upgrade (no ArgoCD) hits the same immutable-Job
       problem: argocd.argoproj.io/hook is ArgoCD-only, plain helm ignores it.
       helm.sh/hook is the native equivalent — coexists safely, each controller
       reads only its own annotation. */}}
    helm.sh/hook: post-install,pre-upgrade
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
    {{- with $migrations.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  backoffLimit: {{ $migrations.backoffLimit | default 6 }}
  activeDeadlineSeconds: {{ $migrations.activeDeadlineSeconds | default 600 }}
  ttlSecondsAfterFinished: {{ $migrations.ttlSecondsAfterFinished | default 259200 }}
  template:
    metadata:
      labels:
        {{- include "midaz-tracer.labels" (dict "context" . "name" (printf "%s-migrations" .Values.tracer.name) ) | nindent 8 }}
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      {{- with .Values.tracer.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tracer.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if $migrations.waitForPostgres.enabled }}
      initContainers:
        {{- include "midaz-tracer.migrationsWaitForPostgresContainer" . | nindent 8 }}
      {{- end }}
      containers:
        {{- include "midaz-tracer.migrationsRunnerContainer" . | nindent 8 }}
      {{- with .Values.tracer.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tracer.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tracer.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
