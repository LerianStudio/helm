{{- include "lerian-common.service" (dict
      "service" .Values.service
      "name" (include "product-console.fullname" .)
      "namespace" (include "product-console.namespace" .)
      "labels" (include "product-console.labels" .)
      "selector" (include "product-console.selectorLabels" .)
    ) }}
