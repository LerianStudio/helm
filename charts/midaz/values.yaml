# Default values for midaz-ledger.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

image:
  repository: lerianstudio/midaz-ledger
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 200m
  #   memory: 256Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations:
  - key: "midaz.io/midaz"
    operator: "Equal"
    effect: "NoSchedule"

affinity: {}
  # nodeAffinity:
  #   requiredDuringSchedulingIgnoredDuringExecution:
  #     nodeSelectorTerms:
  #     - matchExpressions:
  #       - key: midaz.io/owner
  #         operator: In
  #         values:
  #         - midaz

karpenter:
  enabled: false
  instance_types: ["c7a.large", "m7a.large", "c7a.xlarge", "m7a.xlarge"]
  cluster:
    name: "midaz-eks-control-plane-dev"
    iam_role_name: "midaz-eks-control-plane-dev"

configmap:
  ENV_NAME: "local"
  SERVER_PORT: "3000"
  SERVER_ADDRESS: ":3000"
  MONGO_HOST: "mongo-mongodb.midaz.svc.cluster.local"
  MONGO_NAME: "admin"
  MONGO_USER: "midaz"
  MONGO_PORT: "27017"
  REDIS_HOST: "redis-headless.midaz.svc.cluster.local"
  REDIS_PORT: "6379"
  DB_HOST: "postgres-postgresql.midaz.svc.cluster.local"
  DB_USER: "midaz"
  DB_NAME: "ledger"
  DB_PORT: "5432"
  DB_REPLICA_HOST: "postgres-postgresql.midaz.svc.cluster.local"
  DB_REPLICA_USER: "midaz"
  DB_REPLICA_NAME: "ledger"
  DB_REPLICA_PORT: "5432"
  REPLICATION_USER: "replicator"
  USER_EXECUTE_COMMAND: "postgres"
  LOG_LEVEL: "debug"
  MONGO_PASSWORD: "lerian"
  DB_PASSWORD: "lerian"
  DB_REPLICA_PASSWORD: "lerian"
  REPLICATION_PASSWORD: "lerian"

secrets: {}

redis:
  enabled: true
  architecture: standalone
  auth:
    enabled: false

postgresql:
  enabled: true
  auth:
    enablePostgresUser: false
    username: "midaz"
    password: "lerian"
    database: "ledger"
    replicationUsername: replicator
    replicationPassword: "lerian"

mongodb:
  enabled: true
  auth:
    enabled: true
    rootPassword: lerian
    username: midaz
    password: lerian
    database: ledger

# externalRedis:
#   enabled: false
#   architecture: standalone
#   auth:
#     enabled: false

# externalPostgresql:
#   enabled: false
#   global:
#     postgresql:
#       postgresqlPassword: mypassword

# externalMongodb:
#   enabled: false
#   auth:
#     enabled: true
#     rootPassword: rootpassword
#     username: myuser
#     password: mypassword
#     database: mydatabase