{{- if and .Values.ledger.enabled (not .Values.ledger.useExistingSecret) }}
{{- $pg := .Values.postgresql | default dict }}
{{- $pgAuth := $pg.auth | default dict }}
{{- $pgInternal := or (and (ne (toString $pg.enabled) "false") (not $pg.external)) $pgAuth.existingSecret }}
{{- $mongo := .Values.mongodb | default dict }}
{{- $mongoAuth := $mongo.auth | default dict }}
{{- $mongoInternal := or (and (ne (toString $mongo.enabled) "false") (not $mongo.external)) $mongoAuth.existingSecret }}
{{- $vk := .Values.valkey | default dict }}
{{- $vkAuth := $vk.auth | default dict }}
{{- $vkInternal := or (and (ne (toString $vk.enabled) "false") (not $vk.external) $vkAuth.enabled) $vkAuth.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "midaz-ledger.fullname" . }}
type: Opaque
data:
  # =============================================================================
  # INFRA SECRETS (single-sourced from Bitnami subchart Secrets)
  # -----------------------------------------------------------------------------
  # PostgreSQL (DB_*), MongoDB (MONGO_*), and Valkey (REDIS_PASSWORD) passwords are
  # single-sourced from the Bitnami subchart Secrets (<release>-postgresql /
  # -mongodb / -valkey) and injected via secretKeyRef on the ledger Deployment.
  # They are NOT written here for the internal/default case. Each is only emitted
  # below when the matching backend is EXTERNAL (subchart disabled or .external=true)
  # without an existingSecret override, and the operator supplied the value.
  # See docs/helm-chart-standard.md "Single-Source Infra Secrets".
  # =============================================================================
  {{- if and (not $pgInternal) .Values.ledger.secrets.DB_ONBOARDING_PASSWORD }}
  DB_ONBOARDING_PASSWORD: {{ .Values.ledger.secrets.DB_ONBOARDING_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $pgInternal) .Values.ledger.secrets.DB_ONBOARDING_REPLICA_PASSWORD }}
  DB_ONBOARDING_REPLICA_PASSWORD: {{ .Values.ledger.secrets.DB_ONBOARDING_REPLICA_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $mongoInternal) .Values.ledger.secrets.MONGO_ONBOARDING_PASSWORD }}
  MONGO_ONBOARDING_PASSWORD: {{ .Values.ledger.secrets.MONGO_ONBOARDING_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $pgInternal) .Values.ledger.secrets.DB_TRANSACTION_PASSWORD }}
  DB_TRANSACTION_PASSWORD: {{ .Values.ledger.secrets.DB_TRANSACTION_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $pgInternal) .Values.ledger.secrets.DB_TRANSACTION_REPLICA_PASSWORD }}
  DB_TRANSACTION_REPLICA_PASSWORD: {{ .Values.ledger.secrets.DB_TRANSACTION_REPLICA_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $mongoInternal) .Values.ledger.secrets.MONGO_TRANSACTION_PASSWORD }}
  MONGO_TRANSACTION_PASSWORD: {{ .Values.ledger.secrets.MONGO_TRANSACTION_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $mongoInternal) .Values.ledger.secrets.MONGO_CRM_PASSWORD }}
  MONGO_CRM_PASSWORD: {{ .Values.ledger.secrets.MONGO_CRM_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $mongoInternal) .Values.ledger.secrets.MONGO_FEES_PASSWORD }}
  MONGO_FEES_PASSWORD: {{ .Values.ledger.secrets.MONGO_FEES_PASSWORD | b64enc | quote }}
  {{- end }}
  {{- if and (not $vkInternal) .Values.ledger.secrets.REDIS_PASSWORD }}
  REDIS_PASSWORD: {{ .Values.ledger.secrets.REDIS_PASSWORD | b64enc | quote }}
  {{- end }}

  # =============================================================================
  # LCRYPTO (application-level hash/encrypt keys protecting CRM holder PII)
  # =============================================================================
  {{- /* Required from 4.x on with KMS_VENDOR=none: the unified binary serves CRM
       in-process and initializes the legacy cipher from these keys at boot, so an
       empty pair fails with an AES key-size error. 3.x ledger images have no CRM
       and never read them, so the requirement is gated on the image tag. Kept as a
       template comment so the rendered Secret (and its checksum) is unchanged for
       3.x releases. */ -}}
  {{- $ledgerIsV4 := include "midaz.tagIsV4" (.Values.ledger.image.tag | default .Chart.AppVersion) }}
  {{- $requireLcrypto := and $ledgerIsV4 (ne (.Values.ledger.configmap.KMS_VENDOR | default "none") "hashicorp-vault") }}
  {{- /* The encrypt key is hex-decoded into aes.NewCipher, so its format is checked
       whenever a 4.x ledger would read it. Skipped for 3.x, which never does. */ -}}
  {{- if $ledgerIsV4 }}
  {{- include "midaz.validateLcryptoKey" (dict "name" "ledger.secrets.LCRYPTO_ENCRYPT_SECRET_KEY" "key" (.Values.ledger.secrets.LCRYPTO_ENCRYPT_SECRET_KEY | default "")) }}
  {{- end }}
  {{- if $requireLcrypto }}
  LCRYPTO_HASH_SECRET_KEY: {{ required "ledger.secrets.LCRYPTO_HASH_SECRET_KEY is required for midaz 4.x ledger images when KMS_VENDOR=none (the CRM cipher fails to initialize without it)" .Values.ledger.secrets.LCRYPTO_HASH_SECRET_KEY | b64enc | quote }}
  LCRYPTO_ENCRYPT_SECRET_KEY: {{ required "ledger.secrets.LCRYPTO_ENCRYPT_SECRET_KEY is required for midaz 4.x ledger images when KMS_VENDOR=none (the CRM cipher fails to initialize without it)" .Values.ledger.secrets.LCRYPTO_ENCRYPT_SECRET_KEY | b64enc | quote }}
  {{- else }}
  {{- if .Values.ledger.secrets.LCRYPTO_HASH_SECRET_KEY }}
  LCRYPTO_HASH_SECRET_KEY: {{ .Values.ledger.secrets.LCRYPTO_HASH_SECRET_KEY | b64enc | quote }}
  {{- end }}
  {{- if .Values.ledger.secrets.LCRYPTO_ENCRYPT_SECRET_KEY }}
  LCRYPTO_ENCRYPT_SECRET_KEY: {{ .Values.ledger.secrets.LCRYPTO_ENCRYPT_SECRET_KEY | b64enc | quote }}
  {{- end }}
  {{- end }}

  # =============================================================================
  # KMS (Vault AppRole credential; only when KMS_VENDOR=hashicorp-vault)
  # =============================================================================
  {{- if eq (.Values.ledger.configmap.KMS_VENDOR | default "none") "hashicorp-vault" }}
  KMS_VAULT_SECRET_ID: {{ required "ledger.secrets.KMS_VAULT_SECRET_ID is required when KMS_VENDOR=hashicorp-vault (or use ledger.useExistingSecret)" .Values.ledger.secrets.KMS_VAULT_SECRET_ID | b64enc | quote }}
  {{- else if .Values.ledger.secrets.KMS_VAULT_SECRET_ID }}
  KMS_VAULT_SECRET_ID: {{ .Values.ledger.secrets.KMS_VAULT_SECRET_ID | b64enc | quote }}
  {{- end }}

  # =============================================================================
  # SHARED SECRETS (RabbitMQ — operator-provided, see README "Known limitation")
  # =============================================================================
  RABBITMQ_DEFAULT_PASS: {{ required "ledger.secrets.RABBITMQ_DEFAULT_PASS is required" .Values.ledger.secrets.RABBITMQ_DEFAULT_PASS | b64enc | quote }}
  RABBITMQ_CONSUMER_PASS: {{ required "ledger.secrets.RABBITMQ_CONSUMER_PASS is required" .Values.ledger.secrets.RABBITMQ_CONSUMER_PASS | b64enc | quote }}

  # =============================================================================
  # MULTI-TENANT SECRETS (lerian-common.multiTenant.secret — fail-fast when enabled)
  # =============================================================================
  {{- with (include "lerian-common.multiTenant.secret" (dict
        "context" . "secrets" .Values.ledger.secrets
        "secretName" (include "midaz-ledger.fullname" .)
        "valuesPrefix" "ledger.secrets." "mode" "data"
        "enabled" (eq (include "midaz.isTrue" (.Values.ledger.configmap.MULTI_TENANT_ENABLED | default "false")) "true")
        "useExistingSecret" .Values.ledger.useExistingSecret)) }}
  {{- . | nindent 2 }}
  {{- end }}

  # =============================================================================
  # SERVICE DISCOVERY (lib-service-discovery — Consul ACL token, operator-provided)
  # =============================================================================
  SD_TOKEN: {{ .Values.ledger.secrets.SD_TOKEN | default "" | b64enc | quote }}

  # =============================================================================
  # STREAMING SECRETS (lerian-common.streaming.secret — SASL password required when
  # a SASL mechanism is configured; TLS CA optional; both operator-provided)
  # =============================================================================
  {{- $streamingEnabled := eq (include "midaz.isTrue" (.Values.ledger.configmap.STREAMING_ENABLED | default "false")) "true" }}
  {{- $saslMech := (((.Values.global | default dict).streaming | default dict).saslMechanism | default "") }}
  {{- if hasKey .Values.ledger.configmap "STREAMING_SASL_MECHANISM" }}{{- $saslMech = index .Values.ledger.configmap "STREAMING_SASL_MECHANISM" }}{{- end }}
  {{- $saslUser := (((.Values.global | default dict).streaming | default dict).saslUsername | default "") }}
  {{- if hasKey .Values.ledger.configmap "STREAMING_SASL_USERNAME" }}{{- $saslUser = index .Values.ledger.configmap "STREAMING_SASL_USERNAME" }}{{- end }}
  {{- with (include "lerian-common.streaming.secret" (dict "context" $ "enabled" $streamingEnabled "useExistingSecret" .Values.ledger.useExistingSecret "secrets" .Values.ledger.secrets "mode" "data" "saslMechanism" $saslMech "saslUsername" $saslUser "valuesPrefix" "ledger.secrets." "secretName" (include "midaz-ledger.fullname" .))) }}
  {{- . | nindent 2 }}
  {{- end }}
{{- end }}
