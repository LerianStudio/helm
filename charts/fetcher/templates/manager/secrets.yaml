{{- if not .Values.manager.useExistingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fetcher-manager.fullname" . }}
  labels:
    {{- include "fetcher-manager.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.manager.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
