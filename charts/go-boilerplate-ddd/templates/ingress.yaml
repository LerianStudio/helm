{{- if and .Values.boilerplate.enabled .Values.boilerplate.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "boilerplate.fullname" . }}
  namespace: {{ include "global.namespace" . }}
  labels:
    {{- include "boilerplate.labels" (dict "context" . "component" .Values.boilerplate.name "name" .Values.boilerplate.name) | nindent 4 }}
  {{- with .Values.boilerplate.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.boilerplate.ingress.className }}
  ingressClassName: {{ .Values.boilerplate.ingress.className }}
  {{- end }}
  {{- if .Values.boilerplate.ingress.tls }}
  tls:
    {{- range .Values.boilerplate.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.boilerplate.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "boilerplate.fullname" $ }}
                port:
                  number: {{ $.Values.boilerplate.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
