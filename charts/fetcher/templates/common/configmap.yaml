apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fetcher.fullname" . }}-common
  labels:
    {{- include "fetcher.labels" . | nindent 4 }}
data:
  # Dependency connections via lerian-common masks/helpers; everything else an
  # escape-hatch passthrough — see fetcher.commonConfigmapData.
  {{- include "fetcher.commonConfigmapData" . | nindent 2 }}
