{{- if and .Values.dictHubVsync.enabled .Values.dictHubVsync.serviceAccount.create }}
{{- $component := "dict-hub-vsync" }}
{{- $values := .Values.dictHubVsync }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "plugin-br-pix-lerian.componentServiceAccountName" (dict "context" $ "component" $component "componentValues" $values) }}
  namespace: {{ include "global.namespace" . }}
  labels:
    {{- include "plugin-br-pix-lerian.labels" (dict "context" $ "component" $component) | nindent 4 }}
  {{- with $values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
