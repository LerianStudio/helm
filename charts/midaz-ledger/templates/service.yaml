apiVersion: v1
kind: Service
metadata:
  name: {{ include "midaz-ledger.fullname" . }}
  labels:
    {{- include "midaz-ledger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "midaz-ledger.selectorLabels" . | nindent 4 }}
