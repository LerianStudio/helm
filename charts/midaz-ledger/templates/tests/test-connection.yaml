apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "midaz-ledger.fullname" . }}-test-connection"
  labels:
    {{- include "midaz-ledger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "midaz-ledger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
