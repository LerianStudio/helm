{{- if and .Values.boilerplate.enabled .Values.boilerplate.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "boilerplate.fullname" . }}
  namespace: {{ include "global.namespace" . }}
  labels:
    {{- include "boilerplate.labels" (dict "context" . "component" .Values.boilerplate.name "name" .Values.boilerplate.name) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "boilerplate.fullname" . }}
  minReplicas: {{ .Values.boilerplate.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.boilerplate.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.boilerplate.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.boilerplate.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.boilerplate.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.boilerplate.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
