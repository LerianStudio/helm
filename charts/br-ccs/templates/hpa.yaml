{{- if and .Values.brCcs.enabled .Values.brCcs.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "br-ccs.fullname" . }}
  namespace: {{ include "global.namespace" . }}
  labels:
    {{- include "br-ccs.labels" (dict "context" . "component" .Values.brCcs.name "name" .Values.brCcs.name ) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "br-ccs.fullname" . }}
  minReplicas: {{ .Values.brCcs.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.brCcs.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.brCcs.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.brCcs.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.brCcs.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.brCcs.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
