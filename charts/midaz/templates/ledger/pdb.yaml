{{- if and .Values.ledger.enabled .Values.ledger.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "midaz-ledger.fullname" . }}
  labels:
    {{- include "midaz.labels" (dict "context" . "component" .Values.ledger.name "name" .Values.ledger.name ) | nindent 4 }}
  {{- with .Values.ledger.pdb.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  {{- /* Presence-based so an explicit 0 (maxUnavailable: 0 = block all disruptions,
     minAvailable: 0 = allow all) is honored; a PDB must carry exactly one of the two. */ -}}
  {{- if hasKey .Values.ledger.pdb "maxUnavailable" }}
  maxUnavailable: {{ .Values.ledger.pdb.maxUnavailable }}
  {{- else if hasKey .Values.ledger.pdb "minAvailable" }}
  minAvailable: {{ .Values.ledger.pdb.minAvailable }}
  {{- else }}
  minAvailable: 1
  {{- end }}
  selector:
    matchLabels:
      {{- include "midaz.selectorLabels" (dict "context" . "name" .Values.ledger.name ) | nindent 6 }}
{{- end }}
