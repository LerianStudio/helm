{{- if .Values.configmap }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "midaz-ledger.fullname" . }}
data:
  {{- with .Values.configmap }}
    {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}